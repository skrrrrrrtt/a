-- enter backrooms properly
local Lib = require(game:GetService("ReplicatedStorage"):WaitForChild("Library").Client)

local function Teleport(cf)
    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = cf
end

-- enter backrooms via teleport pad
local function EnterBackrooms()
    if Lib.InstancingCmds.PlayerIsInInstance() then
        if Lib.InstancingCmds.GetInstanceID() == "Backrooms" then return end
        Lib.InstancingCmds.Leave()
        task.wait(1)
    end
    for _, v in pairs(workspace.__THINGS.Instances.Backrooms.Teleports:GetChildren()) do
        if v.Name == "Enter" then Teleport(v.CFrame) end
    end
end

EnterBackrooms()
repeat task.wait(0.5) until Lib.InstancingCmds.PlayerIsInInstance()

-- wait for ALL rooms to finish generating
repeat task.wait() until workspace.__THINGS.__INSTANCE_CONTAINER.Active.Backrooms:FindFirstChild("GeneratedBackrooms")

local genBackrooms = workspace.__THINGS.__INSTANCE_CONTAINER.Active.Backrooms.GeneratedBackrooms
local lastCheck = os.time()
local lastSize = #genBackrooms:GetChildren()

while task.wait() do
    local size = #genBackrooms:GetChildren()
    if size ~= lastSize then
        lastCheck = os.time()
        lastSize = size
    end
    if os.time() - lastCheck > 2 and size > 0 then break end
end

print("rooms loaded: " .. #genBackrooms:GetChildren())

-- now scan eggs
local RS = game:GetService("ReplicatedStorage")
local eggsFolder = RS.__DIRECTORY.Eggs["Backrooms Update 2"]

local multiToEgg = {}
for _, egg in pairs(eggsFolder:GetChildren()) do
    local multi = tonumber(egg.Name:match("(%d+)x$"))
    local eggName = egg.Name:match("%| (.+) %d+x$")
    if multi and eggName then
        if not multiToEgg[multi] then multiToEgg[multi] = eggName end
    end
end

local found = {}
for _, room in pairs(genBackrooms:GetChildren()) do
    local roomID = room:GetAttribute("RoomID") or ""
    local multi = room:GetAttribute("EggMultiplier")
    if roomID:lower():find("egg") and multi then
        local eggModel = room:FindFirstChild("EggModel", true)
        local part = eggModel and eggModel:FindFirstChildWhichIsA("BasePart")
        local eggName = multiToEgg[multi] or ("Unknown")
        table.insert(found, {
            name = eggName,
            multi = multi,
            part = part,
            deep = room:GetAttribute("DeepRoom"),
            roomID = roomID
        })
        print("EGG: " .. eggName .. " [" .. multi .. "x] deep=" .. tostring(room:GetAttribute("DeepRoom")))
    end
end

table.sort(found, function(a,b) return a.multi < b.multi end)
print("TOTAL: " .. #found .. " egg rooms")

-- TP UI
local lp = game.Players.LocalPlayer
local sg = Instance.new("ScreenGui")
sg.Name = "BackroomEggTP"
sg.ResetOnSpawn = false
sg.Parent = game:GetService("CoreGui")

local main = Instance.new("Frame", sg)
main.Size = UDim2.new(0, 320, 0, 440)
main.Position = UDim2.new(0.5, -160, 0.5, -220)
main.BackgroundColor3 = Color3.fromRGB(22,22,22)
main.BorderSizePixel = 0
main.Active = true
main.Draggable = true
Instance.new("UICorner", main).CornerRadius = UDim.new(0,8)

local titleBar = Instance.new("Frame", main)
titleBar.Size = UDim2.new(1,0,0,38)
titleBar.BackgroundColor3 = Color3.fromRGB(15,15,15)
titleBar.BorderSizePixel = 0
Instance.new("UICorner", titleBar).CornerRadius = UDim.new(0,8)

local titleLbl = Instance.new("TextLabel", titleBar)
titleLbl.Size = UDim2.new(1,-40,1,0)
titleLbl.Position = UDim2.new(0,10,0,0)
titleLbl.BackgroundTransparency = 1
titleLbl.TextColor3 = Color3.fromRGB(255,200,0)
titleLbl.Font = Enum.Font.GothamBold
titleLbl.TextSize = 13
titleLbl.TextXAlignment = Enum.TextXAlignment.Left
titleLbl.Text = "🟡 Backroom Eggs — " .. #found .. " rooms"

local closeBtn = Instance.new("TextButton", titleBar)
closeBtn.Size = UDim2.new(0,28,0,28)
closeBtn.Position = UDim2.new(1,-32,0,5)
closeBtn.BackgroundColor3 = Color3.fromRGB(180,30,30)
closeBtn.TextColor3 = Color3.new(1,1,1)
closeBtn.Text = "X"
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 12
closeBtn.BorderSizePixel = 0
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0,4)
closeBtn.MouseButton1Click:Connect(function() sg:Destroy() end)

local scroll = Instance.new("ScrollingFrame", main)
scroll.Size = UDim2.new(1,-10,1,-48)
scroll.Position = UDim2.new(0,5,0,43)
scroll.BackgroundTransparency = 1
scroll.ScrollBarThickness = 4
scroll.BorderSizePixel = 0
scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
scroll.CanvasSize = UDim2.new(0,0,0,0)
Instance.new("UIListLayout", scroll).Padding = UDim.new(0,4)

if #found == 0 then
    local empty = Instance.new("TextLabel", scroll)
    empty.Size = UDim2.new(1,-8,0,40)
    empty.BackgroundTransparency = 1
    empty.TextColor3 = Color3.fromRGB(180,180,180)
    empty.Text = "No egg rooms found"
    empty.Font = Enum.Font.Gotham
    empty.TextSize = 12
end

for _, egg in pairs(found) do
    local col
    if egg.multi >= 50 then col = Color3.fromRGB(255,60,60)
    elseif egg.multi >= 20 then col = Color3.fromRGB(255,140,0)
    elseif egg.multi >= 10 then col = Color3.fromRGB(255,220,0)
    else col = Color3.fromRGB(80,210,80) end

    local btn = Instance.new("TextButton", scroll)
    btn.Size = UDim2.new(1,-8,0,44)
    btn.BackgroundColor3 = Color3.fromRGB(38,38,38)
    btn.TextColor3 = Color3.new(1,1,1)
    btn.BorderSizePixel = 0
    btn.TextXAlignment = Enum.TextXAlignment.Left
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 12
    btn.Text = "    " .. egg.name .. (egg.deep and "  🔵" or "")
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0,5)

    local accent = Instance.new("Frame", btn)
    accent.Size = UDim2.new(0,5,1,0)
    accent.BackgroundColor3 = col
    accent.BorderSizePixel = 0
    Instance.new("UICorner", accent).CornerRadius = UDim.new(0,4)

    local multiTag = Instance.new("TextLabel", btn)
    multiTag.Size = UDim2.new(0,55,1,0)
    multiTag.Position = UDim2.new(1,-60,0,0)
    multiTag.BackgroundTransparency = 1
    multiTag.TextColor3 = col
    multiTag.Font = Enum.Font.GothamBold
    multiTag.TextSize = 14
    multiTag.Text = egg.multi .. "x"

    btn.MouseButton1Click:Connect(function()
        local c = lp.Character
        if not c or not c:FindFirstChild("HumanoidRootPart") then return end
        -- re-find part in case it loaded after sweep
        local freshRoom
        for _, room in pairs(genBackrooms:GetChildren()) do
            if room:GetAttribute("RoomID") == egg.roomID and room:GetAttribute("EggMultiplier") == egg.multi then
                freshRoom = room
                break
            end
        end
        local freshModel = freshRoom and freshRoom:FindFirstChild("EggModel", true)
        local freshPart = freshModel and freshModel:FindFirstChildWhichIsA("BasePart")
        local target = freshPart or egg.part
        if not target or not target.Parent then
            btn.Text = "    [GONE] " .. egg.name
            btn.BackgroundColor3 = Color3.fromRGB(60,20,20)
            return
        end
        c.HumanoidRootPart.CFrame = target.CFrame + Vector3.new(0,5,0)
        btn.BackgroundColor3 = Color3.fromRGB(20,60,20)
        task.delay(1.5, function() btn.BackgroundColor3 = Color3.fromRGB(38,38,38) end)
    end)
end
